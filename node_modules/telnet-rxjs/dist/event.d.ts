import { Connection } from './connection';
export declare abstract class Event {
    timestamp: Date;
    constructor();
}
export declare namespace Event {
    class ConnectionChange extends Event {
        connection: Connection;
        constructor(connection: Connection);
    }
    class Connecting extends ConnectionChange {
    }
    class Connected extends ConnectionChange {
    }
    class Disconnecting extends ConnectionChange {
    }
    class Disconnected extends ConnectionChange {
    }
    class Data extends Event {
        data: string;
        constructor(data: string);
    }
    class Command extends Event {
        command: number[];
        constructor(command: number[]);
    }
    class Server extends Event {
    }
    class Starting extends Server {
    }
    class Started extends Server {
    }
    class Ending extends Server {
    }
    class Ended extends Server {
    }
}
