/// <reference types="node" />
import * as net from 'net';
import * as tls from 'tls';
import { ReplaySubject } from 'rxjs/ReplaySubject';
import { Event } from './event';
export declare class Server extends ReplaySubject<Event.Server> {
    private options;
    private server;
    private connections;
    constructor(options?: any);
    start(): Promise<net.Server | tls.Server>;
    stop(): Promise<void>;
    private serverNoTls(hostUrl);
    private serverTls(hostUrl);
    private connectionFactory(socket);
}
